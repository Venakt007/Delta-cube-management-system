# 🎨 System Architecture Diagram

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USERS                                    │
├─────────────┬──────────────┬──────────────┬─────────────────────┤
│  Candidate  │  Recruiter   │    Admin     │   System Admin      │
│  (Public)   │  (Login)     │   (Login)    │    (Login)          │
└──────┬──────┴──────┬───────┴──────┬───────┴──────┬──────────────┘
       │             │              │              │
       │             │              │              │
       ▼             ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    REACT FRONTEND                                │
│                  (http://localhost:3000)                         │
├─────────────┬──────────────┬──────────────┬─────────────────────┤
│ Application │  Recruiter   │    Admin     │  System Admin       │
│    Form     │  Dashboard   │  Dashboard   │   Dashboard         │
│     (/)     │ (/recruiter) │   (/admin)   │ (/system-admin)     │
└──────┬──────┴──────┬───────┴──────┬───────┴──────┬──────────────┘
       │             │              │              │
       │ HTTP/REST   │              │              │
       ▼             ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   EXPRESS BACKEND                                │
│                  (http://localhost:5000)                         │
├─────────────┬──────────────┬──────────────┬─────────────────────┤
│ /api/       │ /api/        │ /api/admin   │ /api/system-admin   │
│ applications│ applications │              │                     │
│ (public)    │ (auth)       │ (auth)       │ (auth)              │
└──────┬──────┴──────┬───────┴──────┬───────┴──────┬──────────────┘
       │             │              │              │
       │             │              │              │
       ▼             ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    MIDDLEWARE LAYER                              │
├─────────────┬──────────────┬──────────────┬─────────────────────┤
│   Multer    │     JWT      │    CORS      │   Validation        │
│ (File       │ (Auth)       │ (Security)   │  (Input)            │
│  Upload)    │              │              │                     │
└──────┬──────┴──────┬───────┴──────┬───────┴──────┬──────────────┘
       │             │              │              │
       ▼             ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   BUSINESS LOGIC                                 │
├─────────────┬──────────────┬──────────────┬─────────────────────┤
│   Resume    │     JD       │   Status     │   Referral          │
│   Parser    │   Matching   │  Management  │   Tracking          │
│  (3-Tier)   │    (AI)      │              │                     │
└──────┬──────┴──────┬───────┴──────┬───────┴──────┬──────────────┘
       │             │              │              │
       ▼             ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   POSTGRESQL DATABASE                            │
│                 (resume_management)                              │
├─────────────────────────────────────────────────────────────────┤
│  Tables:                                                         │
│  - users (id, email, password, role, name)                      │
│  - applications (id, name, email, skills, status, etc.)         │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│                   FILE STORAGE                                   │
│                   (uploads/ folder)                              │
├─────────────────────────────────────────────────────────────────┤
│  - Resumes (PDF, DOC, DOCX)                                     │
│  - ID Proofs (PDF, JPG, PNG)                                    │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagrams

### 1. Application Submission Flow

```
Candidate
   │
   │ 1. Fill form + upload files
   ▼
Application Form
   │
   │ 2. POST /api/applications/submit
   ▼
Backend API
   │
   ├─→ 3a. Save files to uploads/
   │
   ├─→ 3b. Parse resume (3-tier)
   │      ├─→ Tier 1: Structured (Fast)
   │      ├─→ Tier 2: Regex (Fast)
   │      └─→ Tier 3: AI (Slow)
   │
   └─→ 4. Save to database
       │
       ▼
   PostgreSQL
       │
       │ 5. Return success
       ▼
   Candidate sees confirmation
```

### 2. Recruiter Upload Flow

```
Recruiter
   │
   │ 1. Login
   ▼
JWT Token
   │
   │ 2. Upload resumes (bulk)
   ▼
Recruiter Dashboard
   │
   │ 3. POST /api/applications/upload-bulk
   ▼
Backend API
   │
   ├─→ 4. Validate files (20 max)
   │
   ├─→ 5. Parse each resume
   │      └─→ 3-tier parsing
   │
   └─→ 6. Save to database
       │   (uploaded_by = recruiter_id)
       ▼
   PostgreSQL
       │
       │ 7. Return results
       ▼
   Dashboard shows success/errors
```

### 3. Status Update Flow

```
Recruiter
   │
   │ 1. Change status dropdown
   ▼
Recruiter Dashboard
   │
   │ 2. PATCH /api/applications/resumes/:id/status
   ▼
Backend API
   │
   ├─→ 3. Verify ownership
   │
   ├─→ 4. Update database
   │      ├─→ recruitment_status OR
   │      └─→ placement_status
   │
   └─→ 5. If "Onboarded"
       │   └─→ Hidden from admin active tab
       │   └─→ Visible in admin onboarded tab
       ▼
   PostgreSQL
       │
       │ 6. Return updated record
       ▼
   Dashboard updates UI
```

### 4. JD Matching Flow

```
Admin/System Admin
   │
   │ 1. Paste job description
   ▼
Dashboard
   │
   │ 2. POST /api/admin/jd-match
   ▼
Backend API
   │
   ├─→ 3. Send to OpenAI
   │      └─→ Extract: skills, experience, location
   │
   ├─→ 4. Get resumes from database
   │      └─→ Filter by user role
   │
   ├─→ 5. Calculate match for each
   │      ├─→ Skills match (60%)
   │      ├─→ Experience match (30%)
   │      └─→ Location match (10%)
   │
   └─→ 6. Sort by percentage
       │
       │ 7. Return matches
       ▼
   Dashboard displays results
```

### 5. Referral Tracking Flow

```
Social Media Post
   │
   │ Link: /?ref=LinkedIn
   ▼
Candidate clicks
   │
   │ URL parameter captured
   ▼
Application Form
   │
   │ referral_source = "LinkedIn"
   ▼
Form Submission
   │
   │ POST /api/applications/submit
   ▼
Backend saves
   │
   │ referral_source stored in DB
   ▼
System Admin Dashboard
   │
   │ Shows "LinkedIn" badge
   ▼
Track campaign effectiveness
```

## 🔐 Authentication Flow

```
User
   │
   │ 1. Enter email + password
   ▼
Login Page
   │
   │ 2. POST /api/auth/login
   ▼
Backend API
   │
   ├─→ 3. Find user in database
   │
   ├─→ 4. Compare password (bcrypt)
   │
   ├─→ 5. Generate JWT token
   │      └─→ Payload: { id, email, role }
   │
   └─→ 6. Return token + user data
       │
       ▼
   Frontend stores in localStorage
       │
       │ 7. Redirect based on role
       ▼
   ├─→ admin → /admin
   ├─→ recruiter → /recruiter
   └─→ system_admin → /system-admin
```

## 📊 Database Relationships

```
┌─────────────────┐
│     users       │
├─────────────────┤
│ id (PK)         │◄─────┐
│ email           │      │
│ password        │      │
│ role            │      │
│ name            │      │
└─────────────────┘      │
                         │
                         │ uploaded_by (FK)
                         │
                    ┌────┴──────────────┐
                    │   applications    │
                    ├───────────────────┤
                    │ id (PK)           │
                    │ name              │
                    │ email             │
                    │ phone             │
                    │ skills            │
                    │ experience_years  │
                    │ recruitment_status│
                    │ placement_status  │
                    │ referral_source   │
                    │ uploaded_by (FK)  │
                    │ resume_url        │
                    │ parsed_data       │
                    └───────────────────┘
```

## 🎯 Module Interaction

```
┌──────────────────────────────────────────────────────────┐
│                    PUBLIC ACCESS                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │         Module 1: Application Form                 │  │
│  │  - Submit resumes                                  │  │
│  │  - No login required                               │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
                         │
                         │ Saves to database
                         ▼
┌──────────────────────────────────────────────────────────┐
│                  AUTHENTICATED ACCESS                     │
│  ┌────────────────────────────────────────────────────┐  │
│  │      Module 2: Recruiter Dashboard                 │  │
│  │  - Upload resumes (manual + bulk)                  │  │
│  │  - View own resumes                                │  │
│  │  - Update status                                   │  │
│  │  - Edit/Delete                                     │  │
│  └────────────────────────────────────────────────────┘  │
│                         │                                 │
│                         │ Uploads to database             │
│                         ▼                                 │
│  ┌────────────────────────────────────────────────────┐  │
│  │         Module 3: Admin Dashboard                  │  │
│  │  - View ALL resumes                                │  │
│  │  - Advanced filtering                              │  │
│  │  - JD matching                                     │  │
│  │  - View onboarded separately                       │  │
│  └────────────────────────────────────────────────────┘  │
│                         │                                 │
│                         │ Views all data                  │
│                         ▼                                 │
│  ┌────────────────────────────────────────────────────┐  │
│  │      Module 4: System Admin Dashboard              │  │
│  │  - Upload resumes                                  │  │
│  │  - Track referral sources                          │  │
│  │  - JD matching                                     │  │
│  │  - View own resumes only                           │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

## 🔄 Resume Parsing (3-Tier System)

```
Resume File
   │
   ▼
┌─────────────────────────────────────────┐
│         TIER 1: Structured Parsing      │
│  - Look for section headers             │
│  - Extract under each section           │
│  - Speed: < 1 second                    │
│  - Cost: FREE                           │
│  - Success: 60% of resumes              │
└─────────────┬───────────────────────────┘
              │
              │ If fails
              ▼
┌─────────────────────────────────────────┐
│         TIER 2: Regex Parsing           │
│  - Pattern matching                     │
│  - 40+ tech keywords                    │
│  - Speed: < 1 second                    │
│  - Cost: FREE                           │
│  - Success: 35% of resumes              │
└─────────────┬───────────────────────────┘
              │
              │ If fails
              ▼
┌─────────────────────────────────────────┐
│         TIER 3: AI Parsing              │
│  - OpenAI GPT-3.5-turbo                 │
│  - Highest accuracy                     │
│  - Speed: 3-7 seconds                   │
│  - Cost: $0.002 per resume              │
│  - Success: 5% of resumes               │
└─────────────┬───────────────────────────┘
              │
              ▼
        Parsed Data
```

## 📈 Cost Comparison

```
Before (All AI):
100 resumes × $0.002 = $0.20

After (3-Tier):
60 resumes → Tier 1 (FREE)
35 resumes → Tier 2 (FREE)
5 resumes → Tier 3 ($0.01)
Total: $0.01

Savings: 95%! 💰
```

---

**This diagram shows the complete system architecture and data flow for the Resume Management System.**
